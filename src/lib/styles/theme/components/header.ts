import type { ComponentStyleConfig } from '@chakra-ui/react';

export const Header: ComponentStyleConfig = {
  baseStyle: {
    w: 'full',
    top: '0',
    position: 'sticky',
    zIndex: 999,
    align: 'center',
  },
};
